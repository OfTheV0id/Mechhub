# 关键页面视觉回归清单

用于确保 Tailwind CSS 组件精简后，核心页面在视觉细节上保持一致。

## 登录/注册（AuthPage）
- [ ] 卡片整体圆角与阴影层级保持一致（外层 3xl 圆角、内层卡片阴影）。  
- [ ] 登录/注册切换胶囊按钮对齐与滑块宽度一致。  
- [ ] 输入框左右内边距与图标间距视觉平衡。  
- [ ] 主按钮高度、投影与 hover 颜色一致。  
- [ ] 社交按钮 icon 对齐、背景 hover 变化一致。  

## 落地页（LandingPage）
- [ ] 顶层卡片圆角与背景色一致（hero 圆角）。  
- [ ] 主标题字重、行高与字距一致。  
- [ ] “Start Now” 主要按钮 hover 缩放与阴影一致。  
- [ ] “Login” 描边按钮 hover 反色一致。  
- [ ] 右侧图片区域居中与浮动动画节奏一致。  

## 侧边栏（Sidebar）
- [ ] “新对话” 按钮高度、字号与阴影一致。  
- [ ] 最近对话列表 hover 反馈一致。  
- [ ] 用户信息卡片圆角与头像描边一致。  
- [ ] 退出登录按钮 hover 色阶一致。

## 本轮代码一致性结果（2026-02-04）
- [x] 按钮/输入/卡片样式已统一到 shared variants（`Button`/`Input`/`Card`）。  
- [x] 关键页面硬编码色值已收敛到 design token（文本、边框、填充、状态色）。  
- [x] 关键页面散点尺寸已收敛到 size/font token（卡片高、图片区尺寸、meta 字号等）。  
- [x] 图标 `size` 已统一改用共享常量（`src/lib/ui-constants.ts`）。  
- [ ] 需人工逐页视觉确认（Auth/Landing/Sidebar 的 hover、间距、对齐、动画节奏）。

## 人工验收记录模板
- 执行日期：`YYYY-MM-DD`  
- 执行环境：`Desktop` / `Mobile`  
- 分支或提交：`<git-sha>`  
- 结果汇总：`通过项 X/Y，待调整 Z 项`

### 待调整项记录（逐条补充）
- 页面：`AuthPage | LandingPage | Sidebar`  
- 检查项：`粘贴上方清单原文`  
- 现象：`具体可复现描述`  
- 建议：`调整方向（token/variant/spacing/icon）`  
- 状态：`待处理 | 已修复 | 已验证`

## 首轮人工回归记录（样例）
- 执行日期：`2026-02-04`  
- 执行环境：`Desktop (1440x900)`  
- 分支或提交：`working tree (uncommitted)`  
- 结果汇总：`通过项 11/14，待调整 3 项`

### 待调整项（样例）
- 页面：`AuthPage`  
- 检查项：`输入框左右内边距与图标间距视觉平衡。`  
- 现象：`密码框右侧显隐按钮与文本区域在小宽度下略显拥挤。`  
- 建议：`Input 增加一个“withTrailingIcon”尺寸变体，统一右内边距。`  
- 状态：`待处理`

- 页面：`LandingPage`  
- 检查项：`右侧图片区域居中与浮动动画节奏一致。`  
- 现象：`超宽屏下图片视觉中心略偏上。`  
- 建议：`右侧容器增加垂直微调 token（例如 translateY）。`  
- 状态：`待处理`

- 页面：`Sidebar`  
- 检查项：`“新对话” 按钮高度、字号与阴影一致。`  
- 现象：`图标和文字视觉重心略偏上。`  
- 建议：`按钮内部使用统一 line-height 或微调 items-center + py token。`  
- 状态：`待处理`
